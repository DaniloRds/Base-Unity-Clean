-- local cfg = module("cfg/items")
-- local itemList = vRP.itemList()

-- for k,v in pairs(itemList) do
-- 	id_item = k
-- 	name = vRP.itemNameList(id_item) 
-- 	weight = vRP.itemWeightList(id_item)
-- 	vRP.defInventoryItem(id_item,name,weight)
-- end

